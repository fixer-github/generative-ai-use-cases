/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * LiteLLM API
 * Proxy Server to call 100+ LLMs in the OpenAI format. [**Customize Swagger Docs**](https://docs.litellm.ai/docs/proxy/enterprise#swagger-docs---custom-routes--branding)

👉 [```LiteLLM Admin Panel on /ui```](/ui/). Create, Edit Keys with SSO

💸 [```LiteLLM Model Cost Map```](https://models.litellm.ai/).

🔎 [```LiteLLM Model Hub```](/ui/model_hub_table). See available models on the proxy. [**Docs**](https://docs.litellm.ai/docs/proxy/model_hub)
 * OpenAPI spec version: 1.74.11
 */
import type {
  EmbeddingsEmbeddingsPostParams,
  EmbeddingsV1EmbeddingsPostParams,
  HTTPValidationError,
} from '.././models';

/**
 * Follows the exact same API spec as `OpenAI's Embeddings API https://platform.openai.com/docs/api-reference/embeddings`

```bash
curl -X POST http://localhost:4000/v1/embeddings 
-H "Content-Type: application/json" 
-H "Authorization: Bearer sk-1234" 
-d '{
    "model": "text-embedding-ada-002",
    "input": "The quick brown fox jumps over the lazy dog"
}'
```
 * @summary Embeddings
 */
export type embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse200 = {
  data: unknown;
  status: 200;
};

export type embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse422 = {
  data: HTTPValidationError;
  status: 422;
};

export type embeddingsOpenaiDeploymentsModelEmbeddingsPostResponseComposite =
  | embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse200
  | embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse422;

export type embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse =
  embeddingsOpenaiDeploymentsModelEmbeddingsPostResponseComposite & {
    headers: Headers;
  };

export const getEmbeddingsOpenaiDeploymentsModelEmbeddingsPostUrl = (
  model: string | null
) => {
  return `/openai/deployments/${model}/embeddings`;
};

export const embeddingsOpenaiDeploymentsModelEmbeddingsPost = async (
  model: string | null,
  options?: RequestInit
): Promise<embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse> => {
  const res = await fetch(
    getEmbeddingsOpenaiDeploymentsModelEmbeddingsPostUrl(model),
    {
      ...options,
      method: 'POST',
    }
  );

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse['data'] =
    body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as embeddingsOpenaiDeploymentsModelEmbeddingsPostResponse;
};

/**
 * Follows the exact same API spec as `OpenAI's Embeddings API https://platform.openai.com/docs/api-reference/embeddings`

```bash
curl -X POST http://localhost:4000/v1/embeddings 
-H "Content-Type: application/json" 
-H "Authorization: Bearer sk-1234" 
-d '{
    "model": "text-embedding-ada-002",
    "input": "The quick brown fox jumps over the lazy dog"
}'
```
 * @summary Embeddings
 */
export type embeddingsEnginesModelEmbeddingsPostResponse200 = {
  data: unknown;
  status: 200;
};

export type embeddingsEnginesModelEmbeddingsPostResponse422 = {
  data: HTTPValidationError;
  status: 422;
};

export type embeddingsEnginesModelEmbeddingsPostResponseComposite =
  | embeddingsEnginesModelEmbeddingsPostResponse200
  | embeddingsEnginesModelEmbeddingsPostResponse422;

export type embeddingsEnginesModelEmbeddingsPostResponse =
  embeddingsEnginesModelEmbeddingsPostResponseComposite & {
    headers: Headers;
  };

export const getEmbeddingsEnginesModelEmbeddingsPostUrl = (
  model: string | null
) => {
  return `/engines/${model}/embeddings`;
};

export const embeddingsEnginesModelEmbeddingsPost = async (
  model: string | null,
  options?: RequestInit
): Promise<embeddingsEnginesModelEmbeddingsPostResponse> => {
  const res = await fetch(getEmbeddingsEnginesModelEmbeddingsPostUrl(model), {
    ...options,
    method: 'POST',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: embeddingsEnginesModelEmbeddingsPostResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as embeddingsEnginesModelEmbeddingsPostResponse;
};

/**
 * Follows the exact same API spec as `OpenAI's Embeddings API https://platform.openai.com/docs/api-reference/embeddings`

```bash
curl -X POST http://localhost:4000/v1/embeddings 
-H "Content-Type: application/json" 
-H "Authorization: Bearer sk-1234" 
-d '{
    "model": "text-embedding-ada-002",
    "input": "The quick brown fox jumps over the lazy dog"
}'
```
 * @summary Embeddings
 */
export type embeddingsEmbeddingsPostResponse200 = {
  data: unknown;
  status: 200;
};

export type embeddingsEmbeddingsPostResponse422 = {
  data: HTTPValidationError;
  status: 422;
};

export type embeddingsEmbeddingsPostResponseComposite =
  | embeddingsEmbeddingsPostResponse200
  | embeddingsEmbeddingsPostResponse422;

export type embeddingsEmbeddingsPostResponse =
  embeddingsEmbeddingsPostResponseComposite & {
    headers: Headers;
  };

export const getEmbeddingsEmbeddingsPostUrl = (
  params?: EmbeddingsEmbeddingsPostParams
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/embeddings?${stringifiedParams}`
    : `/embeddings`;
};

export const embeddingsEmbeddingsPost = async (
  params?: EmbeddingsEmbeddingsPostParams,
  options?: RequestInit
): Promise<embeddingsEmbeddingsPostResponse> => {
  const res = await fetch(getEmbeddingsEmbeddingsPostUrl(params), {
    ...options,
    method: 'POST',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: embeddingsEmbeddingsPostResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as embeddingsEmbeddingsPostResponse;
};

/**
 * Follows the exact same API spec as `OpenAI's Embeddings API https://platform.openai.com/docs/api-reference/embeddings`

```bash
curl -X POST http://localhost:4000/v1/embeddings 
-H "Content-Type: application/json" 
-H "Authorization: Bearer sk-1234" 
-d '{
    "model": "text-embedding-ada-002",
    "input": "The quick brown fox jumps over the lazy dog"
}'
```
 * @summary Embeddings
 */
export type embeddingsV1EmbeddingsPostResponse200 = {
  data: unknown;
  status: 200;
};

export type embeddingsV1EmbeddingsPostResponse422 = {
  data: HTTPValidationError;
  status: 422;
};

export type embeddingsV1EmbeddingsPostResponseComposite =
  | embeddingsV1EmbeddingsPostResponse200
  | embeddingsV1EmbeddingsPostResponse422;

export type embeddingsV1EmbeddingsPostResponse =
  embeddingsV1EmbeddingsPostResponseComposite & {
    headers: Headers;
  };

export const getEmbeddingsV1EmbeddingsPostUrl = (
  params?: EmbeddingsV1EmbeddingsPostParams
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/v1/embeddings?${stringifiedParams}`
    : `/v1/embeddings`;
};

export const embeddingsV1EmbeddingsPost = async (
  params?: EmbeddingsV1EmbeddingsPostParams,
  options?: RequestInit
): Promise<embeddingsV1EmbeddingsPostResponse> => {
  const res = await fetch(getEmbeddingsV1EmbeddingsPostUrl(params), {
    ...options,
    method: 'POST',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: embeddingsV1EmbeddingsPostResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as embeddingsV1EmbeddingsPostResponse;
};
