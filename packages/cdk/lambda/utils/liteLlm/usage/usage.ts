/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  UsageAudioSpeechesParams,
  UsageAudioTranscriptionsParams,
  UsageCodeInterpreterSessionsParams,
  UsageCompletionsParams,
  UsageCostsParams,
  UsageEmbeddingsParams,
  UsageImagesParams,
  UsageModerationsParams,
  UsageResponse,
  UsageVectorStoresParams,
} from '.././schemas';

/**
 * @summary Get costs details for the organization.
 */
export type usageCostsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageCostsResponseComposite = usageCostsResponse200;

export type usageCostsResponse = usageCostsResponseComposite & {
  headers: Headers;
};

export const getUsageCostsUrl = (params: UsageCostsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/costs?${stringifiedParams}`
    : `/organization/costs`;
};

export const usageCosts = async (
  params: UsageCostsParams,
  options?: RequestInit
): Promise<usageCostsResponse> => {
  const res = await fetch(getUsageCostsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageCostsResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageCostsResponse;
};

/**
 * @summary Get audio speeches usage details for the organization.
 */
export type usageAudioSpeechesResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageAudioSpeechesResponseComposite = usageAudioSpeechesResponse200;

export type usageAudioSpeechesResponse = usageAudioSpeechesResponseComposite & {
  headers: Headers;
};

export const getUsageAudioSpeechesUrl = (params: UsageAudioSpeechesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/audio_speeches?${stringifiedParams}`
    : `/organization/usage/audio_speeches`;
};

export const usageAudioSpeeches = async (
  params: UsageAudioSpeechesParams,
  options?: RequestInit
): Promise<usageAudioSpeechesResponse> => {
  const res = await fetch(getUsageAudioSpeechesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageAudioSpeechesResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageAudioSpeechesResponse;
};

/**
 * @summary Get audio transcriptions usage details for the organization.
 */
export type usageAudioTranscriptionsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageAudioTranscriptionsResponseComposite =
  usageAudioTranscriptionsResponse200;

export type usageAudioTranscriptionsResponse =
  usageAudioTranscriptionsResponseComposite & {
    headers: Headers;
  };

export const getUsageAudioTranscriptionsUrl = (
  params: UsageAudioTranscriptionsParams
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/audio_transcriptions?${stringifiedParams}`
    : `/organization/usage/audio_transcriptions`;
};

export const usageAudioTranscriptions = async (
  params: UsageAudioTranscriptionsParams,
  options?: RequestInit
): Promise<usageAudioTranscriptionsResponse> => {
  const res = await fetch(getUsageAudioTranscriptionsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageAudioTranscriptionsResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageAudioTranscriptionsResponse;
};

/**
 * @summary Get code interpreter sessions usage details for the organization.
 */
export type usageCodeInterpreterSessionsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageCodeInterpreterSessionsResponseComposite =
  usageCodeInterpreterSessionsResponse200;

export type usageCodeInterpreterSessionsResponse =
  usageCodeInterpreterSessionsResponseComposite & {
    headers: Headers;
  };

export const getUsageCodeInterpreterSessionsUrl = (
  params: UsageCodeInterpreterSessionsParams
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/code_interpreter_sessions?${stringifiedParams}`
    : `/organization/usage/code_interpreter_sessions`;
};

export const usageCodeInterpreterSessions = async (
  params: UsageCodeInterpreterSessionsParams,
  options?: RequestInit
): Promise<usageCodeInterpreterSessionsResponse> => {
  const res = await fetch(getUsageCodeInterpreterSessionsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageCodeInterpreterSessionsResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageCodeInterpreterSessionsResponse;
};

/**
 * @summary Get completions usage details for the organization.
 */
export type usageCompletionsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageCompletionsResponseComposite = usageCompletionsResponse200;

export type usageCompletionsResponse = usageCompletionsResponseComposite & {
  headers: Headers;
};

export const getUsageCompletionsUrl = (params: UsageCompletionsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/completions?${stringifiedParams}`
    : `/organization/usage/completions`;
};

export const usageCompletions = async (
  params: UsageCompletionsParams,
  options?: RequestInit
): Promise<usageCompletionsResponse> => {
  const res = await fetch(getUsageCompletionsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageCompletionsResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageCompletionsResponse;
};

/**
 * @summary Get embeddings usage details for the organization.
 */
export type usageEmbeddingsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageEmbeddingsResponseComposite = usageEmbeddingsResponse200;

export type usageEmbeddingsResponse = usageEmbeddingsResponseComposite & {
  headers: Headers;
};

export const getUsageEmbeddingsUrl = (params: UsageEmbeddingsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/embeddings?${stringifiedParams}`
    : `/organization/usage/embeddings`;
};

export const usageEmbeddings = async (
  params: UsageEmbeddingsParams,
  options?: RequestInit
): Promise<usageEmbeddingsResponse> => {
  const res = await fetch(getUsageEmbeddingsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageEmbeddingsResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageEmbeddingsResponse;
};

/**
 * @summary Get images usage details for the organization.
 */
export type usageImagesResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageImagesResponseComposite = usageImagesResponse200;

export type usageImagesResponse = usageImagesResponseComposite & {
  headers: Headers;
};

export const getUsageImagesUrl = (params: UsageImagesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/images?${stringifiedParams}`
    : `/organization/usage/images`;
};

export const usageImages = async (
  params: UsageImagesParams,
  options?: RequestInit
): Promise<usageImagesResponse> => {
  const res = await fetch(getUsageImagesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageImagesResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageImagesResponse;
};

/**
 * @summary Get moderations usage details for the organization.
 */
export type usageModerationsResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageModerationsResponseComposite = usageModerationsResponse200;

export type usageModerationsResponse = usageModerationsResponseComposite & {
  headers: Headers;
};

export const getUsageModerationsUrl = (params: UsageModerationsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/moderations?${stringifiedParams}`
    : `/organization/usage/moderations`;
};

export const usageModerations = async (
  params: UsageModerationsParams,
  options?: RequestInit
): Promise<usageModerationsResponse> => {
  const res = await fetch(getUsageModerationsUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageModerationsResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageModerationsResponse;
};

/**
 * @summary Get vector stores usage details for the organization.
 */
export type usageVectorStoresResponse200 = {
  data: UsageResponse;
  status: 200;
};

export type usageVectorStoresResponseComposite = usageVectorStoresResponse200;

export type usageVectorStoresResponse = usageVectorStoresResponseComposite & {
  headers: Headers;
};

export const getUsageVectorStoresUrl = (params: UsageVectorStoresParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/organization/usage/vector_stores?${stringifiedParams}`
    : `/organization/usage/vector_stores`;
};

export const usageVectorStores = async (
  params: UsageVectorStoresParams,
  options?: RequestInit
): Promise<usageVectorStoresResponse> => {
  const res = await fetch(getUsageVectorStoresUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: usageVectorStoresResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as usageVectorStoresResponse;
};
