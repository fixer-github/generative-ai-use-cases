/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  CreateFileRequest,
  DeleteFileResponse,
  ListFilesParams,
  ListFilesResponse,
  OpenAIFile,
} from '.././schemas';

/**
 * @summary Returns a list of files.
 */
export type listFilesResponse200 = {
  data: ListFilesResponse;
  status: 200;
};

export type listFilesResponseComposite = listFilesResponse200;

export type listFilesResponse = listFilesResponseComposite & {
  headers: Headers;
};

export const getListFilesUrl = (params?: ListFilesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/files?${stringifiedParams}`
    : `/files`;
};

export const listFiles = async (
  params?: ListFilesParams,
  options?: RequestInit
): Promise<listFilesResponse> => {
  const res = await fetch(getListFilesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: listFilesResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as listFilesResponse;
};

/**
 * @summary Upload a file that can be used across various endpoints. Individual files can be up to 512 MB, and the size of all files uploaded by one organization can be up to 100 GB.

The Assistants API supports files up to 2 million tokens and of specific file types. See the [Assistants Tools guide](/docs/assistants/tools) for details.

The Fine-tuning API only supports `.jsonl` files. The input also has certain required formats for fine-tuning [chat](/docs/api-reference/fine-tuning/chat-input) or [completions](/docs/api-reference/fine-tuning/completions-input) models.

The Batch API only supports `.jsonl` files up to 200 MB in size. The input also has a specific required [format](/docs/api-reference/batch/request-input).

Please [contact us](https://help.openai.com/) if you need to increase these storage limits.

 */
export type createFileResponse200 = {
  data: OpenAIFile;
  status: 200;
};

export type createFileResponseComposite = createFileResponse200;

export type createFileResponse = createFileResponseComposite & {
  headers: Headers;
};

export const getCreateFileUrl = () => {
  return `/files`;
};

export const createFile = async (
  createFileRequest: CreateFileRequest,
  options?: RequestInit
): Promise<createFileResponse> => {
  const formData = new FormData();
  formData.append(`file`, createFileRequest.file);
  formData.append(`purpose`, createFileRequest.purpose);

  const res = await fetch(getCreateFileUrl(), {
    ...options,
    method: 'POST',
    body: formData,
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: createFileResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createFileResponse;
};

/**
 * @summary Delete a file.
 */
export type deleteFileResponse200 = {
  data: DeleteFileResponse;
  status: 200;
};

export type deleteFileResponseComposite = deleteFileResponse200;

export type deleteFileResponse = deleteFileResponseComposite & {
  headers: Headers;
};

export const getDeleteFileUrl = (fileId: string) => {
  return `/files/${fileId}`;
};

export const deleteFile = async (
  fileId: string,
  options?: RequestInit
): Promise<deleteFileResponse> => {
  const res = await fetch(getDeleteFileUrl(fileId), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: deleteFileResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as deleteFileResponse;
};

/**
 * @summary Returns information about a specific file.
 */
export type retrieveFileResponse200 = {
  data: OpenAIFile;
  status: 200;
};

export type retrieveFileResponseComposite = retrieveFileResponse200;

export type retrieveFileResponse = retrieveFileResponseComposite & {
  headers: Headers;
};

export const getRetrieveFileUrl = (fileId: string) => {
  return `/files/${fileId}`;
};

export const retrieveFile = async (
  fileId: string,
  options?: RequestInit
): Promise<retrieveFileResponse> => {
  const res = await fetch(getRetrieveFileUrl(fileId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: retrieveFileResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as retrieveFileResponse;
};

/**
 * @summary Returns the contents of the specified file.
 */
export type downloadFileResponse200 = {
  data: string;
  status: 200;
};

export type downloadFileResponseComposite = downloadFileResponse200;

export type downloadFileResponse = downloadFileResponseComposite & {
  headers: Headers;
};

export const getDownloadFileUrl = (fileId: string) => {
  return `/files/${fileId}/content`;
};

export const downloadFile = async (
  fileId: string,
  options?: RequestInit
): Promise<downloadFileResponse> => {
  const res = await fetch(getDownloadFileUrl(fileId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: downloadFileResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as downloadFileResponse;
};
