/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  AddUploadPartRequest,
  CompleteUploadRequest,
  CreateUploadRequest,
  Upload,
  UploadPart,
} from '.././schemas';

/**
 * @summary Creates an intermediate [Upload](/docs/api-reference/uploads/object) object
that you can add [Parts](/docs/api-reference/uploads/part-object) to.
Currently, an Upload can accept at most 8 GB in total and expires after an
hour after you create it.

Once you complete the Upload, we will create a
[File](/docs/api-reference/files/object) object that contains all the parts
you uploaded. This File is usable in the rest of our platform as a regular
File object.

For certain `purpose` values, the correct `mime_type` must be specified. 
Please refer to documentation for the 
[supported MIME types for your use case](/docs/assistants/tools/file-search#supported-files).

For guidance on the proper filename extensions for each purpose, please
follow the documentation on [creating a
File](/docs/api-reference/files/create).

 */
export type createUploadResponse200 = {
  data: Upload;
  status: 200;
};

export type createUploadResponseComposite = createUploadResponse200;

export type createUploadResponse = createUploadResponseComposite & {
  headers: Headers;
};

export const getCreateUploadUrl = () => {
  return `/uploads`;
};

export const createUpload = async (
  createUploadRequest: CreateUploadRequest,
  options?: RequestInit
): Promise<createUploadResponse> => {
  const res = await fetch(getCreateUploadUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createUploadRequest),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: createUploadResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createUploadResponse;
};

/**
 * @summary Cancels the Upload. No Parts may be added after an Upload is cancelled.

 */
export type cancelUploadResponse200 = {
  data: Upload;
  status: 200;
};

export type cancelUploadResponseComposite = cancelUploadResponse200;

export type cancelUploadResponse = cancelUploadResponseComposite & {
  headers: Headers;
};

export const getCancelUploadUrl = (uploadId: string) => {
  return `/uploads/${uploadId}/cancel`;
};

export const cancelUpload = async (
  uploadId: string,
  options?: RequestInit
): Promise<cancelUploadResponse> => {
  const res = await fetch(getCancelUploadUrl(uploadId), {
    ...options,
    method: 'POST',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: cancelUploadResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as cancelUploadResponse;
};

/**
 * @summary Completes the [Upload](/docs/api-reference/uploads/object). 

Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.

You can specify the order of the Parts by passing in an ordered list of the Part IDs.

The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed.

 */
export type completeUploadResponse200 = {
  data: Upload;
  status: 200;
};

export type completeUploadResponseComposite = completeUploadResponse200;

export type completeUploadResponse = completeUploadResponseComposite & {
  headers: Headers;
};

export const getCompleteUploadUrl = (uploadId: string) => {
  return `/uploads/${uploadId}/complete`;
};

export const completeUpload = async (
  uploadId: string,
  completeUploadRequest: CompleteUploadRequest,
  options?: RequestInit
): Promise<completeUploadResponse> => {
  const res = await fetch(getCompleteUploadUrl(uploadId), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(completeUploadRequest),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: completeUploadResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as completeUploadResponse;
};

/**
 * @summary Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload. 

Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.

It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete).

 */
export type addUploadPartResponse200 = {
  data: UploadPart;
  status: 200;
};

export type addUploadPartResponseComposite = addUploadPartResponse200;

export type addUploadPartResponse = addUploadPartResponseComposite & {
  headers: Headers;
};

export const getAddUploadPartUrl = (uploadId: string) => {
  return `/uploads/${uploadId}/parts`;
};

export const addUploadPart = async (
  uploadId: string,
  addUploadPartRequest: AddUploadPartRequest,
  options?: RequestInit
): Promise<addUploadPartResponse> => {
  const formData = new FormData();
  formData.append(`data`, addUploadPartRequest.data);

  const res = await fetch(getAddUploadPartUrl(uploadId), {
    ...options,
    method: 'POST',
    body: formData,
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: addUploadPartResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as addUploadPartResponse;
};
