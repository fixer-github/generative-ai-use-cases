/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  Batch,
  CreateBatchBody,
  ListBatchesParams,
  ListBatchesResponse,
} from '.././schemas';

/**
 * @summary Creates and executes a batch from an uploaded file of requests
 */
export type createBatchResponse200 = {
  data: Batch;
  status: 200;
};

export type createBatchResponseComposite = createBatchResponse200;

export type createBatchResponse = createBatchResponseComposite & {
  headers: Headers;
};

export const getCreateBatchUrl = () => {
  return `/batches`;
};

export const createBatch = async (
  createBatchBody: CreateBatchBody,
  options?: RequestInit
): Promise<createBatchResponse> => {
  const res = await fetch(getCreateBatchUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createBatchBody),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: createBatchResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createBatchResponse;
};

/**
 * @summary List your organization's batches.
 */
export type listBatchesResponse200 = {
  data: ListBatchesResponse;
  status: 200;
};

export type listBatchesResponseComposite = listBatchesResponse200;

export type listBatchesResponse = listBatchesResponseComposite & {
  headers: Headers;
};

export const getListBatchesUrl = (params?: ListBatchesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/batches?${stringifiedParams}`
    : `/batches`;
};

export const listBatches = async (
  params?: ListBatchesParams,
  options?: RequestInit
): Promise<listBatchesResponse> => {
  const res = await fetch(getListBatchesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: listBatchesResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as listBatchesResponse;
};

/**
 * @summary Retrieves a batch.
 */
export type retrieveBatchResponse200 = {
  data: Batch;
  status: 200;
};

export type retrieveBatchResponseComposite = retrieveBatchResponse200;

export type retrieveBatchResponse = retrieveBatchResponseComposite & {
  headers: Headers;
};

export const getRetrieveBatchUrl = (batchId: string) => {
  return `/batches/${batchId}`;
};

export const retrieveBatch = async (
  batchId: string,
  options?: RequestInit
): Promise<retrieveBatchResponse> => {
  const res = await fetch(getRetrieveBatchUrl(batchId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: retrieveBatchResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as retrieveBatchResponse;
};

/**
 * @summary Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file.
 */
export type cancelBatchResponse200 = {
  data: Batch;
  status: 200;
};

export type cancelBatchResponseComposite = cancelBatchResponse200;

export type cancelBatchResponse = cancelBatchResponseComposite & {
  headers: Headers;
};

export const getCancelBatchUrl = (batchId: string) => {
  return `/batches/${batchId}/cancel`;
};

export const cancelBatch = async (
  batchId: string,
  options?: RequestInit
): Promise<cancelBatchResponse> => {
  const res = await fetch(getCancelBatchUrl(batchId), {
    ...options,
    method: 'POST',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: cancelBatchResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as cancelBatchResponse;
};
