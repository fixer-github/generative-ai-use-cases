/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  DeleteModelResponse,
  ListModelsResponse,
  Model,
} from '.././schemas';

/**
 * @summary Lists the currently available models, and provides basic information about each one such as the owner and availability.
 */
export type listModelsResponse200 = {
  data: ListModelsResponse;
  status: 200;
};

export type listModelsResponseComposite = listModelsResponse200;

export type listModelsResponse = listModelsResponseComposite & {
  headers: Headers;
};

export const getListModelsUrl = () => {
  return `/models`;
};

export const listModels = async (
  options?: RequestInit
): Promise<listModelsResponse> => {
  const res = await fetch(getListModelsUrl(), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: listModelsResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as listModelsResponse;
};

/**
 * @summary Retrieves a model instance, providing basic information about the model such as the owner and permissioning.
 */
export type retrieveModelResponse200 = {
  data: Model;
  status: 200;
};

export type retrieveModelResponseComposite = retrieveModelResponse200;

export type retrieveModelResponse = retrieveModelResponseComposite & {
  headers: Headers;
};

export const getRetrieveModelUrl = (model: string) => {
  return `/models/${model}`;
};

export const retrieveModel = async (
  model: string,
  options?: RequestInit
): Promise<retrieveModelResponse> => {
  const res = await fetch(getRetrieveModelUrl(model), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: retrieveModelResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as retrieveModelResponse;
};

/**
 * @summary Delete a fine-tuned model. You must have the Owner role in your organization to delete a model.
 */
export type deleteModelResponse200 = {
  data: DeleteModelResponse;
  status: 200;
};

export type deleteModelResponseComposite = deleteModelResponse200;

export type deleteModelResponse = deleteModelResponseComposite & {
  headers: Headers;
};

export const getDeleteModelUrl = (model: string) => {
  return `/models/${model}`;
};

export const deleteModel = async (
  model: string,
  options?: RequestInit
): Promise<deleteModelResponse> => {
  const res = await fetch(getDeleteModelUrl(model), {
    ...options,
    method: 'DELETE',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: deleteModelResponse['data'] = body ? JSON.parse(body) : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as deleteModelResponse;
};
