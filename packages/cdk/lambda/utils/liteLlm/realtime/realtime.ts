/**
 * Generated by orval v7.10.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import type {
  RealtimeSessionCreateRequest,
  RealtimeSessionCreateResponse,
  RealtimeTranscriptionSessionCreateRequest,
  RealtimeTranscriptionSessionCreateResponse,
} from '.././schemas';

/**
 * @summary Create an ephemeral API token for use in client-side applications with the
Realtime API. Can be configured with the same session parameters as the
`session.update` client event.

It responds with a session object, plus a `client_secret` key which contains
a usable ephemeral API token that can be used to authenticate browser clients
for the Realtime API.

 */
export type createRealtimeSessionResponse200 = {
  data: RealtimeSessionCreateResponse;
  status: 200;
};

export type createRealtimeSessionResponseComposite =
  createRealtimeSessionResponse200;

export type createRealtimeSessionResponse =
  createRealtimeSessionResponseComposite & {
    headers: Headers;
  };

export const getCreateRealtimeSessionUrl = () => {
  return `/realtime/sessions`;
};

export const createRealtimeSession = async (
  realtimeSessionCreateRequest: RealtimeSessionCreateRequest,
  options?: RequestInit
): Promise<createRealtimeSessionResponse> => {
  const res = await fetch(getCreateRealtimeSessionUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(realtimeSessionCreateRequest),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: createRealtimeSessionResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createRealtimeSessionResponse;
};

/**
 * @summary Create an ephemeral API token for use in client-side applications with the
Realtime API specifically for realtime transcriptions. 
Can be configured with the same session parameters as the `transcription_session.update` client event.

It responds with a session object, plus a `client_secret` key which contains
a usable ephemeral API token that can be used to authenticate browser clients
for the Realtime API.

 */
export type createRealtimeTranscriptionSessionResponse200 = {
  data: RealtimeTranscriptionSessionCreateResponse;
  status: 200;
};

export type createRealtimeTranscriptionSessionResponseComposite =
  createRealtimeTranscriptionSessionResponse200;

export type createRealtimeTranscriptionSessionResponse =
  createRealtimeTranscriptionSessionResponseComposite & {
    headers: Headers;
  };

export const getCreateRealtimeTranscriptionSessionUrl = () => {
  return `/realtime/transcription_sessions`;
};

export const createRealtimeTranscriptionSession = async (
  realtimeTranscriptionSessionCreateRequest: RealtimeTranscriptionSessionCreateRequest,
  options?: RequestInit
): Promise<createRealtimeTranscriptionSessionResponse> => {
  const res = await fetch(getCreateRealtimeTranscriptionSessionUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(realtimeTranscriptionSessionCreateRequest),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data: createRealtimeTranscriptionSessionResponse['data'] = body
    ? JSON.parse(body)
    : {};

  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createRealtimeTranscriptionSessionResponse;
};
