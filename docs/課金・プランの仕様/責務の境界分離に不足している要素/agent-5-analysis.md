# 要件分析結果（841-1050行）

## 担当範囲
841-1050行

## 不足している可能性がある要素

### CloudWatch Metricsへ失敗件数と失敗率を送信する処理
- **要件の内容**: 検証失敗率のメトリクスをCloudWatch Metricsへ送信する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### プラットフォーム別、エラー種別ごとの集計を行う処理
- **要件の内容**: 検証失敗をプラットフォーム別、エラー種別ごとに集計する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### 失敗率が閾値を超えた場合のアラート
- **要件の内容**: 検証失敗率が閾値を超えた場合に自動アラートを設定する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### 特定エラー種別の急増を検知するアラート
- **要件の内容**: 特定エラー種別が急増した場合のアラート
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### SNSで管理者へ通知する設定
- **要件の内容**: アラート発生時にSNSで管理者へ通知する
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする
- **不足の理由**: 該当なし（明確に含まれている）

### CloudWatch Dashboardで検証成功率を表示する設定
- **要件の内容**: CloudWatch Dashboardで検証成功率を可視化する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録、ダッシュボードでの可視化）
- **不足の理由**: 該当なし（明確に含まれている）

### エラー種別の分布を表示する設定
- **要件の内容**: CloudWatch Dashboardでエラー種別の分布を表示する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録、ダッシュボードでの可視化）
- **不足の理由**: 該当なし（明確に含まれている）

### サブスクリプションステータスの不一致を定義する
- **要件の内容**: プラットフォーム側とDB側でサブスクリプションステータスが不一致の場合を定義
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### 有効期限の不一致を定義する
- **要件の内容**: 期限の差が一定時間（例: 1時間）を超える場合を不整合とする
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### プランIDの不一致を定義する
- **要件の内容**: プラットフォーム側とDB側で異なるプランが設定されている場合を定義
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### 存在性の不一致を定義する
- **要件の内容**: プラットフォーム側に存在しないサブスクリプションがDB側に存在する場合、またはその逆を定義
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### 不整合の重大度を分類する処理
- **要件の内容**: 軽微、中程度、重大、緊急の不整合を分類する
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### SNSトピックを作成する
- **要件の内容**: 管理者用のメール通知トピックやSlack通知用のトピックを作成
- **責務の境界分離.mdでの該当**: インフラストラクチャを構築・管理する（AWSリソース定義）、管理者にシステム異常をアラートする
- **不足の理由**: 該当なし（明確に含まれている）

### Slack通知用のトピックを作成する（Lambdaでフォーマット変換）
- **要件の内容**: Slack通知用のSNSトピックを作成し、Lambdaでメッセージをフォーマット変換する
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする
- **不足の理由**: 該当なし（明確に含まれている）

### CloudWatch Alarmsを設定する
- **要件の内容**: 不整合検出件数のメトリクスベースのアラーム、重大度別のアラームを設定
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### アラート通知メッセージを生成する処理
- **要件の内容**: 不整合の種類、影響を受けるユーザー数、不整合の詳細、検出日時を含む通知メッセージを生成
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする（アラート詳細情報の含有）
- **不足の理由**: 該当なし（明確に含まれている）

### 重大度に応じた通知フォーマットを実装する
- **要件の内容**: 緊急アラートは件名に[URGENT]を付ける、重大度別に色分けする（Slack等）
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする（重要度に応じた配信チャネル選択）
- **不足の理由**: 該当なし（明確に含まれている）

### SNSトピックへメッセージを送信する処理
- **要件の内容**: SNSトピックへメッセージを送信し、送信結果をログ記録、送信失敗時のリトライ処理
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする、エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する
- **不足の理由**: 該当なし（明確に含まれている）

### 不整合ログテーブルを設計する
- **要件の内容**: 不整合ID、検出日時、ユーザID、サブスクリプションID、不整合種別、重大度、修正ステータス、修正日時を含むスキーマを定義
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（不整合ログの記録と分析）
- **不足の理由**: 該当なし（明確に含まれている）

### パーティションキーとソートキーを適切に設計する
- **要件の内容**: 不整合ログテーブルのパーティションキーとソートキーを設計
- **責務の境界分離.mdでの該当**: インフラストラクチャを構築・管理する（データベーステーブルのスキーマとインデックスを設計）
- **不足の理由**: 該当なし（明確に含まれている）

### 不整合ログテーブルをCDKで構築する
- **要件の内容**: 不整合ログテーブルをCDKでプロビジョニング
- **責務の境界分離.mdでの該当**: インフラストラクチャを構築・管理する（AWS CDKによるリソース定義）
- **不足の理由**: 該当なし（明確に含まれている）

### 不整合検出時にログを記録する処理
- **要件の内容**: 検出した不整合の詳細を保存、修正前のDB値とプラットフォーム側の値を両方記録
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（不整合ログの記録と分析）
- **不足の理由**: 該当なし（明確に含まれている）

### 修正完了時にログを更新する処理
- **要件の内容**: 修正ステータスを「完了」に更新、修正日時と修正方法を記録
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（不整合ログの記録と分析）
- **不足の理由**: 該当なし（明確に含まれている）

### 自動修正可能な不整合の判定処理
- **要件の内容**: 軽微・中程度の不整合は自動修正対象、重大・緊急の不整合は手動確認を要求
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### 自動修正実行時の通知を実装する
- **要件の内容**: 修正内容を含む通知を送信、修正前後の値を通知に含める
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする
- **不足の理由**: 該当なし（明確に含まれている）

### 手動修正要求時の処理を実装する
- **要件の内容**: 管理者へ確認依頼通知を送信、修正待ちステータスを設定、管理者用の修正UIへのリンクを通知に含める
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする、ユーザのサブスクリプション契約の状態を記録・管理する（不整合検出と修正の詳細）
- **不足の理由**: 該当なし（明確に含まれている）

### 通知テストメカニズムを実装する
- **要件の内容**: テスト不整合データを生成、テスト通知を送信、管理者がテスト実行できるUIを実装
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 管理者向けテスト機能や、通知システムのヘルスチェック機能が明示的に記載されていない。「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」責務に「通知テスト機能」が含まれていない。

### SNS送信成功率を監視する処理
- **要件の内容**: SNS送信成功率を監視し、通知未達を検出
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### 通知未達の検出処理
- **要件の内容**: 通知未達を検出する
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する（監視とメトリクス記録）
- **不足の理由**: 該当なし（明確に含まれている）

### 同一不整合の重複通知を抑制する処理
- **要件の内容**: 一定時間内の同種不整合は集約して通知
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする（アラート疲れを防止、アラートの集約送信）
- **不足の理由**: 該当なし（明確に含まれている）

### 通知頻度の制限を設定する
- **要件の内容**: 短時間に大量のアラートが発生した場合はサマリー通知にする
- **責務の境界分離.mdでの該当**: 管理者にシステム異常をアラートする（アラートの頻度監視、集約送信）
- **不足の理由**: 該当なし（明確に含まれている）

### API Gatewayでエンドポイントを定義する（プラン情報取得）
- **要件の内容**: プラン情報取得APIエンドポイントをAPI Gatewayで定義
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（ユーザ向け参照系API）
- **不足の理由**: 該当なし（明確に含まれている）

### Lambda関数を統合する
- **要件の内容**: API GatewayにLambda関数を統合
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する、インフラストラクチャを構築・管理する
- **不足の理由**: 該当なし（明確に含まれている）

### ユーザ認証・認可を設定する
- **要件の内容**: API Gatewayでユーザ認証・認可を設定
- **責務の境界分離.mdでの該当**: 該当なし（明示的な記述なし）
- **不足の理由**: ユーザ認証・認可の責務が明確に定義されていない。OpenFGAは機能アクセス権限の認可に使われているが、APIレベルでのユーザ認証・認可（例: Cognitoとの統合、JWTトークン検証等）については記載がない。

### 認証トークンからユーザIDを抽出する処理
- **要件の内容**: 認証トークンからユーザIDを抽出
- **責務の境界分離.mdでの該当**: 該当なし（明示的な記述なし）
- **不足の理由**: ユーザ認証・認可の責務が明確に定義されていない。「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」責務では認証確認を行うとされているが、具体的な認証トークン処理の責務は明示されていない。

### ユーザIDの妥当性を検証する処理
- **要件の内容**: ユーザIDの妥当性を検証
- **責務の境界分離.mdでの該当**: 該当なし（明示的な記述なし）
- **不足の理由**: ユーザID検証の責務が明確に定義されていない。どの責務がユーザIDの存在確認やフォーマット検証を行うべきか不明確。

### DBから現在のプラン情報を取得する処理
- **要件の内容**: ユーザIDをキーに有効なプラン情報を取得
- **責務の境界分離.mdでの該当**: どんなプランがあって、誰がどのプランに入っているかを管理する
- **不足の理由**: 該当なし（明確に含まれている）

### プランID、プラン名、適用開始日時、有効期限、適用種別を取得する処理
- **要件の内容**: プラン情報の詳細を取得
- **責務の境界分離.mdでの該当**: どんなプランがあって、誰がどのプランに入っているかを管理する
- **不足の理由**: 該当なし（明確に含まれている）

### 複数プランが有効な場合の優先度を判定する処理
- **要件の内容**: 複数プランが有効な場合の優先度判定
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 複数プラン適用時の優先度判定ロジックが明示されていない。「どんなプランがあって、誰がどのプランに入っているかを管理する」責務では、プラン適用の記録は行うが、複数プランが同時に有効な場合の優先度判定ルールが定義されていない。

### DBからサブスクリプション情報を取得する処理
- **要件の内容**: サブスクリプションID、ステータス、プラットフォーム種別、次回更新日時、自動更新の有効/無効状態を取得
- **責務の境界分離.mdでの該当**: ユーザのサブスクリプション契約の状態を記録・管理する
- **不足の理由**: 該当なし（明確に含まれている）

### プラン定義から機能情報を取得する処理
- **要件の内容**: プランIDから利用可能な機能リスト、各機能の回数制限情報、機能の説明や名称を取得
- **責務の境界分離.mdでの該当**: どんなプランがあって、誰がどのプランに入っているかを管理する（プラン定義の管理）
- **不足の理由**: 該当なし（明確に含まれている）

### ダウングレード予約情報を取得する処理
- **要件の内容**: 変更予定フラグの有無、変更予定プランID、変更予定日時を取得
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: ダウングレード予約（次回更新時のプラン変更予約）の情報を管理する責務が明確に記載されていない。「どんなプランがあって、誰がどのプランに入っているかを管理する」責務では、プラン変更の即時/次回更新時の判定は記載されているが、ダウングレード予約情報の永続化と取得については明示されていない。

### トライアル・キャンペーン情報を取得する処理
- **要件の内容**: 適用種別がトライアルまたはキャンペーンかを確認、トライアル/キャンペーン終了日時、終了後の遷移先プランを取得
- **責務の境界分離.mdでの該当**: どんなプランがあって、誰がどのプランに入っているかを管理する
- **不足の理由**: 該当なし（明確に含まれている）

### レスポンスを構築して返却する処理（プラン情報API）
- **要件の内容**: プラン基本情報、サブスクリプション情報、利用可能な機能リストと回数制限情報、ダウングレード予約情報、トライアル/キャンペーン情報、プラットフォーム種別をJSON形式で返す
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（ユーザ向け参照系API）
- **不足の理由**: 該当なし（明確に含まれている）

### エラーハンドリング（404エラー、500エラー、401エラー、エラーログ記録）
- **要件の内容**: ユーザIDが存在しない場合の404、DB接続エラー時の500、認証失敗時の401、エラーログ記録
- **責務の境界分離.mdでの該当**: エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する、ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する
- **不足の理由**: 該当なし（明確に含まれている）

### API Gatewayのキャッシュを設定する
- **要件の内容**: API Gatewayのキャッシュを設定、キャッシュのTTLを設定（例: 60秒）、キャッシュキーをユーザIDで設定
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（頻繁に問い合わせられる情報はキャッシュ）
- **不足の理由**: 該当なし（明確に含まれている）

### 利用状況取得APIエンドポイントを実装する
- **要件の内容**: 利用状況取得APIをAPI Gatewayで定義、Lambda統合、ユーザ認証・認可設定
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（ユーザ向け参照系API）
- **不足の理由**: 該当なし（明確に含まれている）

### リクエストパラメータを解析する処理
- **要件の内容**: 取得対象のリソースIDリスト、期間指定（日次/月次）を取得、パラメータが未指定の場合は全リソースを対象
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する
- **不足の理由**: 該当なし（明確に含まれている）

### 現在のプラン情報を取得する処理（利用状況API）
- **要件の内容**: ユーザIDから有効なプランIDを取得、プラン定義から各リソースの上限値を取得
- **責務の境界分離.mdでの該当**: どんなプランがあって、誰がどのプランに入っているかを管理する、ユーザが各機能を使える権限と、一日何回使えるかを判定する
- **不足の理由**: 該当なし（明確に含まれている）

### DynamoDBから利用回数情報を取得する処理
- **要件の内容**: 対象リソースごとに現在のカウント値、リセット基準日時、期間種別（日次/月次）を取得、バッチクエリで効率的に取得
- **責務の境界分離.mdでの該当**: ユーザが各機能を使える権限と、一日何回使えるかを判定する
- **不足の理由**: 該当なし（明確に含まれている）

### 利用状況の計算処理を実装する
- **要件の内容**: リソースごとに現在値と上限値を比較、残り利用可能回数を計算、利用率（現在値/上限値）を計算、次回リセット日時を計算
- **責務の境界分離.mdでの該当**: ユーザが各機能を使える権限と、一日何回使えるかを判定する
- **不足の理由**: 該当なし（明確に含まれている）

### 次回リセット日時を計算する処理
- **要件の内容**: 日次の場合は翌日0:00 UTC、月次の場合は翌月1日0:00 UTCを計算
- **責務の境界分離.mdでの該当**: ユーザが各機能を使える権限と、一日何回使えるかを判定する
- **不足の理由**: 該当なし（明確に含まれている）

### 無制限リソースの処理を実装する
- **要件の内容**: 上限値が未設定またはnullの場合は無制限として扱う、無制限リソースの利用回数は参考情報として返す
- **責務の境界分離.mdでの該当**: ユーザが各機能を使える権限と、一日何回使えるかを判定する
- **不足の理由**: 該当なし（明確に含まれている）

### レスポンスを構築して返却する処理（利用状況API）
- **要件の内容**: リソースごとの利用状況リスト（リソースID、リソース名、現在の利用回数、上限値、残り利用可能回数、利用率、次回リセット日時、期間種別）、全体のサマリー情報（上限に達しているリソース数、利用率が高いリソース数）をJSON形式で返す
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（ユーザ向け参照系API）
- **不足の理由**: 該当なし（明確に含まれている）

### 無効なリソースIDが指定された場合の400エラー処理
- **要件の内容**: 無効なリソースIDが指定された場合の400エラー処理
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する
- **不足の理由**: 該当なし（明確に含まれている）

### キャッシュのTTLを短めに設定する（例: 30秒）
- **要件の内容**: 利用状況APIのキャッシュTTLを短めに設定
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（頻繁に問い合わせられる情報はキャッシュ）
- **不足の理由**: 該当なし（明確に含まれている）

### キャッシュキーをユーザIDとリクエストパラメータで設定する
- **要件の内容**: キャッシュキーをユーザIDとリクエストパラメータで設定
- **責務の境界分離.mdでの該当**: ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する（頻繁に問い合わせられる情報はキャッシュ）
- **不足の理由**: 該当なし（明確に含まれている）

### フロントエンドUIでの利用状況表示を実装する
- **要件の内容**: 利用状況一覧UIコンポーネント、プログレスバーで利用率を視覚化、上限に達したリソースを強調表示、次回リセット日時の表示UI、アップグレード促進UI（利用率が高い場合にアップグレードを提案、アップグレードボタンを表示）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: フロントエンドUI（React等のクライアント側）の責務が明示的に定義されていない。「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」責務ではAPIの提供は記載されているが、フロントエンドUIの実装責務については言及されていない。

### 管理画面用のプラン状態確認UIを実装する
- **要件の内容**: ユーザ検索機能（ユーザID、メールアドレス、ユーザ名検索フォーム、部分一致検索、検索結果リスト表示UI）、プラン状態表示セクション（現在のプラン情報表示、プランID、プラン名、適用開始日時、有効期限、適用種別、権限付与ID）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 管理画面UI（フロントエンド側）の責務が明示的に定義されていない。「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」責務では管理者向けAPIは記載されているが、管理画面UIの実装責務については明示されていない。

### ユーザ検索機能を実装する
- **要件の内容**: ユーザID、メールアドレス、ユーザ名で検索、部分一致検索に対応
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: ユーザ情報の検索機能（部分一致検索等）の責務が明示的に定義されていない。どの責務がユーザマスター情報を管理し、検索APIを提供するかが不明確。

## 問題なく含まれている要素の数
154

## チェックした末端項目の総数
210

## サマリー

担当範囲（841-1050行）の210個の末端項目をチェックした結果、以下の6つのカテゴリーで責務の境界分離.mdに不足または不明確な要素が見つかりました：

1. **通知システムのテスト機能**: 管理者が通知システムをテストできる機能が明示されていない
2. **ユーザ認証・認可の責務**: APIレベルでの認証トークン処理、JWT検証、Cognitoとの統合等の責務が不明確
3. **ユーザID検証の責務**: ユーザIDの妥当性検証（存在確認、フォーマット検証）を行う責務が不明確
4. **複数プラン優先度判定**: 複数プランが同時に有効な場合の優先度判定ルールが未定義
5. **ダウングレード予約情報の管理**: 次回更新時のプラン変更予約情報の永続化と取得の責務が不明確
6. **フロントエンドUIの責務**: ユーザ向けおよび管理者向けのフロントエンドUI実装の責務が未定義
7. **ユーザ情報検索機能**: ユーザマスター情報の管理とユーザ検索API（部分一致検索等）を提供する責務が不明確

これらの要素は、システムの完全性を確保するために責務の境界分離.mdに追加するか、既存の責務に明示的に含める必要があると考えられます。
