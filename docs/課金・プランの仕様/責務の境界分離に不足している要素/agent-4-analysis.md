# 要件分析結果（631-840行）

## 担当範囲
631-840行

## 不足している可能性がある要素

### サービス終了予定日を計算して記録する処理
- **要件の内容**: 解約時に、サービスが実際に終了する日（即時解約の場合は即時、期限終了時解約の場合は有効期限）を計算し記録する
- **責務の境界分離.mdでの該当**: 「購入・変更・解約などの複数ステップの処理を統括する」の解約フローで概念的には触れられているが、サービス終了予定日の明示的な計算・記録処理については記述がない
- **不足の理由**: 解約フローでは「期限まで利用可能」や「即座に終了」という概念は記述されているが、「サービス終了予定日」というフィールドの計算ロジックや記録処理が明示されていない

### 解約理由を記録する処理
- **要件の内容**: ユーザが解約する際の理由を記録する
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 解約フローの記述では解約理由の収集・記録に関する処理が含まれていない。解約理由はユーザフィードバックとして重要だが、どの責務で記録・管理するかが明確でない

### 解約理由へのフィードバック案内を通知に含める処理
- **要件の内容**: 解約確認メールに、解約理由に対するフィードバック案内（アンケートリンク等）を含める
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 通知責務では一般的な通知送信は記述されているが、解約理由のフィードバック案内という具体的なコンテンツの生成・含有についての記述がない

### 通知テンプレートを用意する
- **要件の内容**: 各種通知（解約確認、サービス終了予告等）のテンプレートを用意する
- **責務の境界分離.mdでの該当**: 「ユーザにビジネスイベントを通知する」に「イベントの種類に応じて適切なテンプレートを選択します」とあり、部分的に該当
- **不足の理由**: テンプレートの選択は記述されているが、テンプレートの設計・管理・更新についての責務が明確でない。テンプレート自体をどこで定義し、どのように管理するかが不明

### サービス終了日の事前通知を送信する処理（終了日N日前の対象を検出する処理）
- **要件の内容**: サービス終了日のN日前（例: 3日前）に該当するユーザを検出し、予告通知を送信する
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 「時間駆動のバッチ処理とスケジューリングを実行する」責務はあるが、サービス終了日前の通知という具体的なバッチ処理の記述がない。「ユーザにビジネスイベントを通知する」責務もあるが、時間ベースでの自動トリガー通知についての記述がない

### 解約履歴を記録する処理
- **要件の内容**: 解約ID、ユーザID、プランID、サブスクリプションID、解約日時、解約理由、サービス終了日を含む解約履歴を記録する
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 解約フローは記述されているが、解約履歴という独立したデータエンティティの記録・管理については言及がない。サブスクリプション状態の更新はあるが、解約履歴としての別途記録が明示されていない

### 解約履歴テーブルを設計する（スキーマ定義）
- **要件の内容**: 解約履歴テーブルのスキーマを定義する（解約ID、ユーザID、プランID、サブスクリプションID、解約日時、解約理由、サービス終了日を含む）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: データ記録の仕組みやインフラストラクチャの責務はあるが、解約履歴テーブルという具体的なスキーマ定義についての記述がない

### 解約履歴テーブルをCDKで構築する
- **要件の内容**: CDKを使って解約履歴テーブルを構築する
- **責務の境界分離.mdでの該当**: 「インフラストラクチャを構築・管理する」に一般的なテーブル構築は含まれるが、解約履歴テーブルの具体的な構築については記述なし
- **不足の理由**: 解約履歴テーブルの存在が責務の境界分離.mdに明示されていないため、その構築も明示されていない

### 定期同期の実行基盤を構築する（EventBridge Ruleでスケジュール設定）
- **要件の内容**: EventBridge Ruleでcron式を使って定期同期処理のスケジュールを設定する
- **責務の境界分離.mdでの該当**: 「時間駆動のバッチ処理とスケジューリングを実行する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 同期処理用Lambda関数を実装する
- **要件の内容**: 定期同期処理を実行するLambda関数を実装する
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の定期同期処理に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 同期対象のサブスクリプション一覧を取得する処理
- **要件の内容**: DBから有効なサブスクリプションを取得し、プラットフォーム別に絞り込む
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の定期同期処理に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### プラットフォーム別の同期処理（Stripe/Apple/Google）
- **要件の内容**: 各プラットフォームのAPIを呼び出して最新情報を取得し、レート制限を考慮したバッチ処理を実装
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」と「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」の組み合わせで該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 取得した状態とDB状態の比較処理
- **要件の内容**: サブスクリプションステータス、有効期限、プランIDの一致確認と差分検出
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の「不整合検出と修正の詳細」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 差分の種類を分類する処理（軽微/重大）
- **要件の内容**: 検出した差分を軽微・重大などに分類する
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の「不整合の重大度を分類し、対応方針を判定します」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 不整合データの修正処理
- **要件の内容**: プラットフォーム側のデータを正として、DB更新、バックアップ記録、更新日時と修正理由の記録を行う
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の不整合修正処理に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 関連する権限の再同期処理
- **要件の内容**: プラン情報が変更された場合の権限再付与、ステータスが変更された場合の権限剥奪
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 不整合修正後の権限同期については明示的な記述がない。「ユーザが各機能を使える権限と、一日何回使えるかを判定する」には権限付与の整合性チェックはあるが、サブスクリプション不整合修正時の権限再同期フローが明確でない

### 同期結果のログ記録
- **要件の内容**: 実行日時、対象件数、成功件数、失敗件数、不整合検出件数、修正実行件数などを記録
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 不整合ログを記録する処理
- **要件の内容**: ユーザID、サブスクリプションID、不整合項目、修正前後の値、不整合の重大度を記録
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」の「不整合ログの記録と分析」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 同期処理の監視とエラーハンドリング
- **要件の内容**: 同期処理失敗時のリトライ機構（SQS DLQ）、API呼び出しエラーのハンドリング、タイムアウト設定
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### Webhook受信失敗時のリトライ機構（プラットフォーム側のリトライ設定）
- **要件の内容**: Stripe/Apple/Googleのリトライポリシーを把握・設定
- **責務の境界分離.mdでの該当**: 「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」に部分的に該当するが、プラットフォーム側リトライ設定の確認・設定については明示的な記述なし
- **不足の理由**: Webhookエンドポイントの提供は記述されているが、プラットフォーム側のリトライ設定を確認・管理する責務が明確でない

### Webhook受信の確実性を高める実装（早期の200 OK返却、非同期処理）
- **要件の内容**: 署名検証後すぐに200 OKを返し、実際の処理はSQSへエンキューして別Lambda関数で処理
- **責務の境界分離.mdでの該当**: 「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」に「検証完了後、イベントバスにイベントを送信します」とあり、部分的に該当
- **不足の理由**: イベントバス送信は記述されているが、早期200 OK返却とSQS経由の非同期処理という具体的なパターンが明示されていない（EventBridge経由は記述されているが、SQS経由の記述がない）

### 自前のリトライ機構（Webhook処理失敗の検出、リトライキュー、リトライ処理用Lambda関数）
- **要件の内容**: SQS DLQで処理失敗イベントを捕捉、リトライ用SQSキューの作成、リトライ回数管理、指数バックオフ、リトライ処理用Lambda関数の実装
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### リトライ失敗時の手動リカバリ機構
- **要件の内容**: 失敗したイベント情報をDBに保存、管理者用の手動再実行UI、手動再実行API
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 最大リトライ回数到達時の管理者アラートは記述されているが、手動リカバリのためのUI・API・失敗イベントの永続化については明示的な記述がない

### 失敗イベント一覧を表示するUI
- **要件の内容**: 管理者が失敗したWebhookイベントを確認できるUI
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」責務はあるが、失敗イベント管理UIについての記述がない

### イベント詳細を確認できるUI
- **要件の内容**: 失敗したイベントの詳細（ペイロード、失敗理由等）を確認できるUI
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 失敗イベント管理UIの一部として必要だが、責務の境界分離.mdに記述がない

### 手動再実行ボタンを実装する
- **要件の内容**: 管理者が失敗イベントを手動で再実行できるUI機能
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 失敗イベント管理UIの一部として必要だが、責務の境界分離.mdに記述がない

### 手動再実行API
- **要件の内容**: 失敗イベントIDを受け取り、イベントペイロードを取得して再処理し、結果を記録するAPI
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: 手動リカバリ機構のAPI部分が責務の境界分離.mdに記述されていない

### Webhook受信状況の監視（受信成功率のメトリクス記録、異常検知アラート）
- **要件の内容**: CloudWatch Metricsへの成功/失敗件数送信、プラットフォーム別成功率計測、失敗率閾値アラート、一定時間Webhook未受信アラート
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」の「監視とメトリクス記録」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### レシート検証失敗時のエラーハンドリング（検証失敗の種類を分類する処理）
- **要件の内容**: エラー種別の判定（ネットワークエラー、タイムアウト、認証エラー、レート制限エラー、不正なレシート、プラットフォーム側エラー）、エラー種別ごとの重大度設定
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に部分的に該当
- **不足の理由**: 一般的なエラーハンドリングは記述されているが、レシート検証に特化したエラー分類と重大度設定については明示的な記述がない

### リトライ可能なエラーの処理（自動リトライ処理）
- **要件の内容**: ネットワークエラー、タイムアウトエラー、レート制限エラー時の指数バックオフリトライ、Retry-Afterヘッダーの確認
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### リトライ中の暫定処理
- **要件の内容**: 検証結果を「保留中」ステータスとして記録、ユーザへ処理中メッセージを返す
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の「検証保留中の状態管理」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### リトライ不可能なエラーの処理（不正なレシート検出時）
- **要件の内容**: エラーレスポンスをクライアントへ返却（エラーコードとメッセージ含む）、不正レシートの詳細をログ記録（ユーザID、レシートデータ、エラー理由、検出日時）、セキュリティアラート送信
- **責務の境界分離.mdでの該当**: 部分的に該当
- **不足の理由**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」にログ記録とアラート送信は含まれるが、不正レシート検出という具体的なセキュリティイベントへの対応が明示されていない

### 認証エラー時の処理
- **要件の内容**: APIキーまたは認証情報の異常を検出、即座に管理者へアラート送信、エラーログ記録
- **責務の境界分離.mdでの該当**: 「管理者にシステム異常をアラートする」と「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### プラットフォーム側エラー時の処理
- **要件の内容**: 一時的なエラーとして扱い後でリトライ、エラー継続時間を監視、一定時間継続する場合は管理者へアラート
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 検証失敗時のフォールバック処理（キャッシュされた検証結果の活用）
- **要件の内容**: 過去の検証成功結果をキャッシュ（DynamoDBに保存、TTL設定）、検証失敗時にキャッシュ参照、キャッシュヒット時は暫定的に成功とする、キャッシュ利用をログ記録
- **責務の境界分離.mdでの該当**: 「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」の「レシート検証とキャッシュによるフォールバック処理」に該当
- **不足の理由**: 不足なし（適切にカバーされている）

### 手動検証プロセスへのエスカレーション
- **要件の内容**: 検証保留中のリストを管理、保留レコードをDBに保存（ユーザID、レシートID、保留開始日時、保留理由）、管理者用の検証UI（保留中リスト表示、レシート詳細確認、手動承認/却下ボタン）、手動検証結果の反映処理
- **責務の境界分離.mdでの該当**: 「ユーザのサブスクリプション契約の状態を記録・管理する」の「検証保留中の状態管理」と「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」の管理者向けAPIに該当
- **不足の理由**: 不足なし（適切にカバーされている）

## 問題なく含まれている要素の数
30

## チェックした末端項目の総数
42

## 分析サマリー

担当範囲（631-840行）には合計42個の末端項目があり、そのうち30個は責務の境界分離.mdに適切に含まれていますが、12個の項目について不足または不明確な点が見られました。

主な不足カテゴリ：

1. **解約関連の詳細情報管理**（5項目）
   - 解約理由の記録
   - サービス終了予定日の計算・記録
   - 解約履歴テーブルの設計・構築
   - 解約理由フィードバック案内
   - サービス終了日の事前通知

2. **手動リカバリ機構のUI/API**（5項目）
   - 失敗イベントの永続化
   - 失敗イベント一覧/詳細UI
   - 手動再実行ボタン
   - 手動再実行API

3. **その他**（2項目）
   - 通知テンプレートの管理責務の明確化
   - 不整合修正後の権限再同期フロー
   - Webhook受信のSQS経由非同期処理パターン
   - プラットフォーム側リトライ設定の管理
   - 不正レシート検出のセキュリティ対応

これらの不足要素は、システムの運用品質とユーザ体験に影響する重要な機能であり、責務の境界分離.mdへの追加または既存責務への統合を検討すべきです。
