# 要件分析結果（211-420行）

## 担当範囲
211-420行

## 不足している可能性がある要素

### App Store Connect共有シークレットの環境変数管理
- **要件の内容**: App Store Connectの共有シークレットを環境変数で管理する（211行）
- **責務の境界分離.mdでの該当**: 「環境変数・シークレットを安全に管理する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### App Store Connect Webhook URL登録
- **要件の内容**: App Store ConnectでWebhook URLを登録する（212行）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: Webhook URLの登録は手動運用タスク（外部プラットフォーム側の設定）であり、システムの責務として明示されていない。ただし、これは自動化できない運用作業の可能性もある

### Google Play Console Webhook URL登録
- **要件の内容**: Google Play ConsoleでWebhook URLを登録する（221行）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: Webhook URLの登録は手動運用タスク（外部プラットフォーム側の設定）であり、システムの責務として明示されていない。ただし、これは自動化できない運用作業の可能性もある

### キャンセル理由のユーザアンケート実施
- **要件の内容**: ユーザアンケートを実施する仕組みを用意する（316行）
- **責務の境界分離.mdでの該当**: 該当なし
- **不足の理由**: ユーザアンケートの実施という機能は、通知機能とは異なり、フィードバック収集に特化した責務が必要。「ユーザにビジネスイベントを通知する」では一方向の通知のみであり、双方向のアンケート機能は含まれていない

### サービス停止通知の送信
- **要件の内容**: サービス停止通知を送信する処理を実装する（293行）
- **責務の境界分離.mdでの該当**: 「ユーザにビジネスイベントを通知する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### プラン変更時の変更種別判定（プラン内容の変更検知）
- **要件の内容**: プラン内容の変更を検知して再適用する処理を実装する（420行）
- **責務の境界分離.mdでの該当**: 「購入・変更・解約などの複数ステップの処理を統括する」で部分的に実現可能
- **不足の理由**: Webhook経由でプラン内容の変更を検知して再適用する具体的な処理フローが、統括責務に明示されていない。プラン変更フローは存在するが、「Webhook経由でプラン内容が変更された場合の検知と再適用」という自動処理が明確でない

### タイムアウト検出用のLambda関数実装
- **要件の内容**: タイムアウト検出用のLambda関数を実装する（368行）
- **責務の境界分離.mdでの該当**: 「時間駆動のバッチ処理とスケジューリングを実行する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### EventBridge Ruleでタイムアウト検出を定期実行
- **要件の内容**: EventBridge Ruleで定期実行する設定を行う（369行）
- **責務の境界分離.mdでの該当**: 「時間駆動のバッチ処理とスケジューリングを実行する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### タイムアウトしたイベントの再処理またはアラート
- **要件の内容**: タイムアウトしたイベントを再処理またはアラートする処理を実装する（370行）
- **責務の境界分離.mdでの該当**: 「エラー時の再試行、監視、ログ記録でシステムの安定稼働を保証する」および「管理者にシステム異常をアラートする」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### デフォルトプランの種類の決定
- **要件の内容**: デフォルトプランの種類（Free等）を決める（374行）
- **責務の境界分離.mdでの該当**: 「どんなプランがあって、誰がどのプランに入っているかを管理する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### デフォルトプランの有効期限ポリシーの決定
- **要件の内容**: デフォルトプランの有効期限ポリシーを決める（無期限/期限付き）（375行）
- **責務の境界分離.mdでの該当**: 「どんなプランがあって、誰がどのプランに入っているかを管理する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### デフォルトプランで提供する機能と制限の定義
- **要件の内容**: デフォルトプランで提供する機能と制限を定義する（376行）
- **責務の境界分離.mdでの該当**: 「どんなプランがあって、誰がどのプランに入っているかを管理する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### 課金プラットフォームの商品IDからプランIDへのマッピング処理
- **要件の内容**: 課金プラットフォームの商品IDからプランIDへのマッピング処理を実装する（398行）
- **責務の境界分離.mdでの該当**: 「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### マッピングテーブルのDB準備
- **要件の内容**: マッピングテーブルをDBに用意する（399行）
- **責務の境界分離.mdでの該当**: 「Stripe/Apple/Googleの決済システムとのやり取りを一本化する」および「データ記録の仕組み」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

### エクスポート処理の非同期実行（大量データ対応）
- **要件の内容**: エクスポート処理は非同期で実行し、大量データでもタイムアウトしないようにする（該当行は420行以降の可能性があるが、責務の境界分離.mdの392行に記載あり）
- **責務の境界分離.mdでの該当**: 「ユーザと管理者が情報を見たり操作するためのAPI・画面を提供する」で実現可能
- **不足の理由**: 該当責務に含まれると判断（不足なし）

## チェック結果サマリー

### 問題なく含まれている要素の数
205個

### 不足している可能性がある要素の数
3個

### チェックした末端項目の総数
208個

## 詳細な分析結果

### 明確に不足している要素

1. **キャンセル理由のユーザアンケート実施機能**（316行）
   - 双方向のフィードバック収集機能が責務定義に含まれていない
   - 「ユーザにビジネスイベントを通知する」は一方向の通知のみ
   - 必要な責務: 「ユーザフィードバック（アンケート、レビュー等）を収集・管理する」

2. **Webhook経由でのプラン内容変更の自動検知と再適用**（420行）
   - プラン変更フローは存在するが、Webhook経由での自動検知・再適用が明示されていない
   - 統括責務の「Webhookイベント処理フロー」に含めるべき処理が欠落

### 運用作業として扱われる可能性のある要素（グレーゾーン）

1. **App Store Connect Webhook URL登録**（212行）
2. **Google Play Console Webhook URL登録**（221行）
   - これらは外部プラットフォーム側の手動設定であり、システムの責務として自動化できない可能性が高い
   - ただし、運用手順書や初期セットアップの一部として文書化される必要がある

## 推奨事項

### 責務の追加を検討すべき領域

1. **ユーザフィードバック管理責務**
   - ユーザアンケートの設計・配信・回収・分析を担当
   - キャンセル理由収集、満足度調査等に活用

2. **統括責務のWebhookイベント処理フローの拡張**
   - プラン内容変更の検知と再適用フローを明示的に追加

### 文書化が必要な運用手順

1. **課金プラットフォーム初期セットアップ手順**
   - App Store ConnectでのWebhook URL登録手順
   - Google Play ConsoleでのWebhook URL登録手順
   - StripeでのWebhook URL登録手順（既存の責務に含まれている可能性あり）
